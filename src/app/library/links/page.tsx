import LinksView from "./LinksView";

export const dynamic = "force-dynamic";

/** Your invoice links on the server, and who has submitted (local-mode gate in layout). */
export default function LinksPage() {
  return <LinksView />;
}
